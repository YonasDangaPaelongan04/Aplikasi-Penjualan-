<Window x:Class="UAS_2823.PilihVoucher"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UAS_2823"
        mc:Ignorable="d"
        Title="PilihVoucher" Height="450" Width="434">
    <Grid>
        <Label Content="Daftar voucher belanja" HorizontalAlignment="Left" Margin="16,22,0,0" VerticalAlignment="Top" Width="147"/>
        <ListBox x:Name="DaftarVoucher" HorizontalAlignment="Left" Height="112" Margin="16,61,0,0" VerticalAlignment="Top" Width="385" SelectionChanged="DaftarVoucherSelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,1">
                        <TextBlock Grid.Row="0" Text="{Binding title}" TextAlignment="Left" FontSize="16" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</Window>